services:
  apple-spyder:
    image: bytecoltx/apple-spyder:latest
    container_name: apple-spyder
    restart: unless-stopped
    ports:
      - "5005:5005"
    volumes:
      - ./config:/app/config
      - ./data:/app/data